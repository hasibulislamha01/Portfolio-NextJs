import React from 'react';

const Services = () => {
    return (
        <section id='services' className='w-full section'>
            services
        </section>
    );
};

export default Services;